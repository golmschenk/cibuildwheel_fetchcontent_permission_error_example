[project]
name = "cibuildwheel_fetchcontent_permission_error_example"
version = "0.1.0"
requires-python = ">=3.8"

[build-system]
requires = ["scikit-build-core", "fypp"]
build-backend = "scikit_build_core.build"

[tool.cibuildwheel]
build = "cp38-*"
